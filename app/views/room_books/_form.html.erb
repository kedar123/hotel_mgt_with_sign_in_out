<%= form_for(@room_book) do |f| %>
  <% if @room_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room_book.errors.count, "error") %> prohibited this room_book from being saved:</h2>

      <ul>
      <% @room_book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
